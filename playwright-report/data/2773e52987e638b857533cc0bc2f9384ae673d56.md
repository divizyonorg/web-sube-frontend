# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: faturalarim.spec.ts >> Faturalarım >> fatura durum badge'leri görünür (Ödendi/Ödenmedi)
- Location: tests\faturalarim.spec.ts:32:7

# Error details

```
Error: page.goto: net::ERR_HTTP_RESPONSE_CODE_FAILURE at https://websube.divizyon.org/Faturalarim
Call log:
  - navigating to "https://websube.divizyon.org/Faturalarim", waiting until "load"

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - generic [ref=e6]:
    - heading "Bu websube.divizyon.org sayfası bulunamıyor" [level=1] [ref=e7]:
      - generic [ref=e8]: Bu websube.divizyon.org sayfası bulunamıyor
    - paragraph [ref=e9]:
      - text: "Şu web adresi için web sayfası bulunamadı:"
      - strong [ref=e10]: https://websube.divizyon.org/Faturalarim
    - generic [ref=e11]: HTTP ERROR 404
  - button "Yeniden Yükle" [ref=e14] [cursor=pointer]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { bug, info } from './bug';
  3  | 
  4  | test.describe('Faturalarım', () => {
  5  | 
  6  |   test.beforeEach(async ({ page }) => {
> 7  |     await page.goto('/Faturalarim');
     |                ^ Error: page.goto: net::ERR_HTTP_RESPONSE_CODE_FAILURE at https://websube.divizyon.org/Faturalarim
  8  |     await page.waitForLoadState('domcontentloaded');
  9  |   });
  10 | 
  11 |   test('sayfa yüklenir', async ({ page }) => {
  12 |     await expect(page).toHaveURL(/Faturalarim/i);
  13 |     await expect(page.locator('h1, h2').first()).toBeVisible({ timeout: 15_000 });
  14 |   });
  15 | 
  16 |   test('özet kartları görünür (Toplam Ödenen, Bekleyen, Sonraki)', async ({ page }) => {
  17 |     await page.locator('h1, h2').first().waitFor({ timeout: 15_000 });
  18 |     for (const label of ['Toplam Ödenen', 'Bekleyen Ödeme', 'Sonraki Ödeme']) {
  19 |       if (await page.locator(`text=${label}`).count() === 0)
  20 |         bug(`"${label}" özet kartı sayfada bulunamadı`);
  21 |     }
  22 |   });
  23 | 
  24 |   test('fatura listesi görünür', async ({ page }) => {
  25 |     await page.locator('h1, h2').first().waitFor({ timeout: 15_000 });
  26 |     const rows = page.locator('table tr').filter({ hasNot: page.locator('th') });
  27 |     const count = await rows.count();
  28 |     info(`Fatura satırı sayısı: ${count}`);
  29 |     if (count === 0) bug('Fatura listesi boş — statik demo veri bile görünmüyor olabilir');
  30 |   });
  31 | 
  32 |   test('fatura durum badge\'leri görünür (Ödendi/Ödenmedi)', async ({ page }) => {
  33 |     await page.locator('h1, h2').first().waitFor({ timeout: 15_000 });
  34 |     const odendiExists   = await page.locator('text=Ödendi').count() > 0;
  35 |     const odenmediExists = await page.locator('text=Ödenmedi').count() > 0;
  36 |     if (!odendiExists && !odenmediExists) bug('Fatura durum badge\'leri (Ödendi/Ödenmedi) sayfada bulunamadı');
  37 |   });
  38 | 
  39 |   test('"Şimdi Öde" butonu ödenmemiş faturada görünür', async ({ page }) => {
  40 |     await page.locator('h1, h2').first().waitFor({ timeout: 15_000 });
  41 |     const odeBtn = page.locator('button:has-text("Şimdi Öde"), a:has-text("Şimdi Öde")').first();
  42 |     if (await odeBtn.count() === 0) {
  43 |       bug('"Şimdi Öde" butonu bulunamadı — ödenmemiş fatura yok veya buton eksik');
  44 |     } else {
  45 |       await expect(odeBtn).toBeVisible();
  46 |     }
  47 |   });
  48 | 
  49 |   test('görüntüleme ve indirme butonları görünür', async ({ page }) => {
  50 |     await page.locator('h1, h2').first().waitFor({ timeout: 15_000 });
  51 |     const viewBtns = page.locator('button[aria-label*="Görüntüle"], button[title*="Görüntüle"], a[href*="fatura"]');
  52 |     info(`Fatura görüntüleme butonu sayısı: ${await viewBtns.count()}`);
  53 |   });
  54 | 
  55 | });
  56 | 
```