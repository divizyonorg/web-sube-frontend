    @page
    @model IndexModel
    @{
        ViewData["Title"] = "Ana Sayfa";
    }

    <div class="rounded-xl border border-gray-200 bg-white p-8 text-center shadow-sm">
        <h1 class="text-3xl font-semibold text-gray-900">Hoş geldiniz</h1>
        <p class="mt-4 text-gray-600">ASP.NET Core Razor Pages + Tailwind + Typed HttpClient yapısı hazır.</p>
        <a class="mt-6 inline-block rounded-lg bg-yellow-600 px-5 py-2.5 text-sm font-medium text-white hover:bg-red-700"
           asp-page="/Clients/Index">Müşteri listesine git</a>
    </div>
